package util;

/**
 * Player roles as defined in the project specification.
 * Chompers collect pellets; Chasers attempt to catch Chompers.
 *
 * Milestone 2: roles will be assigned by the server upon connection.
 */
public enum Role {
    CHOMPER,
    CHASER
}
